	应用程序包依赖于其他的包，但是也具有独立性。所谓依赖性，是对其他包中的代码进行调用，减少重复代码的书写。应用程序包在使用时还需考虑lock file对源代码的控制，lock file确保创建相同配置应用程序使用的包。其独立性则表现为，在命令行或浏览器中直接运行。应用程序包减少了代码的重复书写，但是带来的问题是在lockfile中需要特别注意对application package的限制与约束的说明。
